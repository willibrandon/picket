# Baseline JSON Oracle Input

This fixture exercises Gitleaks-compatible JSON baseline suppression. The
baseline entry intentionally has a different fingerprint and tag list than the
current finding so the oracle proves those fields are not baseline keys.
