{
  "name": "picket-docs",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "scripts": {
    "docs:api-build": "dotnet build ../src/Picket.Rules/Picket.Rules.csproj --configuration Release --framework net10.0 --verbosity minimal && dotnet build ../src/Picket.Engine/Picket.Engine.csproj --configuration Release --framework net10.0 --verbosity minimal && dotnet build ../src/Picket.Report/Picket.Report.csproj --configuration Release --framework net10.0 --verbosity minimal",
    "docs:generate": "pnpm docs:api-build && dotnet run --project ../tools/Picket.Docs/Picket.Docs.csproj",
    "docs:check": "pnpm docs:api-build && dotnet run --project ../tools/Picket.Docs/Picket.Docs.csproj -- --check",
    "prebuild": "pnpm docs:generate",
    "build": "astro build",
    "dev": "astro dev",
    "preview": "astro build && astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "@astrojs/starlight": "0.41.3",
    "astro": "7.0.6",
    "sharp": "0.35.3"
  },
  "devDependencies": {}
}
